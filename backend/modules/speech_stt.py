# modules/speech_stt.py

def transcribe_audio(audio_path: str) -> str:
    # Mock STT logic (since actual Whisper requires heavy dependencies)
    return "This is a mock transcription of the patient's speech."
